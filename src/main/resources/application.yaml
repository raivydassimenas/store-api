spring:
  application:
    name: store-backend
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/postgres-store-api-db
    username: raivydas
    password: Labas123!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    validate-on-migrate: true
